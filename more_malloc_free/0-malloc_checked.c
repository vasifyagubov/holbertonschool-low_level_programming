#include <stdio.h>
#include <stdlib.h>

/**
* *malloc_checked - allocate memory with malloc
*@b:unsigned int type
*Return: return pointer
*
*/

void *malloc_checked(unsigned int b)
{
	int *abc;

	abc = malloc(b);
	if (abc = NULL)
		return (98);
	i

	return (abc);
}
